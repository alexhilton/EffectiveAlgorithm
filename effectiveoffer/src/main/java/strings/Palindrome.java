package strings;

public class Palindrome {
    // Question 18
    public static boolean isPalindrome(String s) {
        if (s == null || s.length() == 0) {
            return false;
        }

        return true;
    }
}
